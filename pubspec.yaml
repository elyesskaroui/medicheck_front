name: flutter_application_1
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_login: ^5.0.0
  cupertino_icons: ^1.0.8
  lottie: ^1.2.1
  http: ^1.3.0  # Gardez seulement cette version plus récente
  shared_preferences: ^2.5.1
  percent_indicator: ^4.2.5
  # Dépendances supplémentaires nécessaires pour votre popup
  google_fonts: ^6.1.0   # Pour améliorer la typographie (optionnel mais recommandé)
  fl_chart: ^0.65.0
  flutter_svg: ^2.0.9  # Ajout du package flutter_svg     # Alternative pour des graphiques plus avancés (optionnel)


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - asset/images/medical_background.png
    - asset/images/stope.jpg
    - asset/images/test.jpg
    - asset/images/logo1.png
    - asset/images/report.jpg
